//Before Modifying the following code such that the object properties are destructured and logged.
const person = {
                      name: “Noam Chomsky”,
                      age: 92
            }
           
           let name = person.name;
           let age = person.age;
           console.log(name);
           console.log(age);
           
           
//After Modifying the following code such that the object properties are destructured and logged.
const {name,age} ={name: “Noam Chomsky”,age: 92}
console.log(name);
console.log(age);	     
